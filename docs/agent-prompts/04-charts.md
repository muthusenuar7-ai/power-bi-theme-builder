# Phase 4 — Charts Prompt

Migrate all chart SVGs into modular React components.

Follow Power BI style rules:
- Single-series: one color only
- Multi-series: multiple colors by series
- Pie/donut/treemap: multiple colors by slice/block

Run `npm run build` and fix all errors.
