import type { Metadata } from 'next'
import { AppShell } from '@/components/layout/AppShell'

export const metadata: Metadata = {
  title: 'Editor | Datacense Power BI Theme Studio',
}

export default function EditorPage() {
  return <AppShell />
}
