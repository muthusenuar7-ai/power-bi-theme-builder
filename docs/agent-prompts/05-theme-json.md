# Phase 5 — Theme JSON Prompt

Implement:
- generateThemeJSON
- downloadThemeJSON
- import existing JSON
- live JSON preview
- validation panel

Power BI rules:
- textClasses only: callout, title, header, label
- visualStyles colors must use solid object format
- all colors must be valid #RRGGBB

Run `npm run build` and fix all errors.
