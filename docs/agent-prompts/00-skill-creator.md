# Skill Creator Prompt

Create reusable project skills for Claude Code and Codex for this repository.

Skills needed:
1. Bootstrap app shell
2. Migrate core TypeScript/store/lib files
3. Build dashboard canvas
4. Migrate SVG charts
5. Implement Power BI theme JSON import/export
6. Run QA/build validation

Use `.claude/skills/<skill-name>/SKILL.md` for Claude Code.
Use `.codex/skills/<skill-name>/SKILL.md` for Codex.

Each skill must include:
- YAML frontmatter
- goal
- files to read
- tasks
- rules
- verification commands
