# Datacense Power BI Theme Studio environment reference
#
# The active product path uses the in-app Studio Preview mimic renderer.
# No Power BI Embedded report IDs, embed URLs, access tokens, tenant IDs,
# client IDs, or client secrets are required for local development or Vercel.
#
# Keep this file as placeholders only. Put any future private values in
# .env.local, which is ignored by git.

# Example placeholder for future optional integrations:
# NEXT_PUBLIC_APP_ENV=development
